import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sentinel.app',
  appName: 'sentinel-inspector',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
